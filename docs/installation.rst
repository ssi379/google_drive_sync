============
Installation
============

At the command line::

    $ easy_install google_drive_sync

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv google_drive_sync
    $ pip install google_drive_sync